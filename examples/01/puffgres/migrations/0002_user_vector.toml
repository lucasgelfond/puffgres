# Migration for user_vector table
version = 2
mapping_name = "user_vector_public"
namespace = "user_vector"
columns = ["id", "email", "name"]

[source]
schema = "public"
table = "users"

[id]
column = "id"
type = "uint"

# Optional: filter which rows to sync
# [membership]
# mode = "dsl"
# predicate = "status = 'active'"

[versioning]
mode = "source_lsn"

[transform]
path = "./transforms/user_vector.ts"
