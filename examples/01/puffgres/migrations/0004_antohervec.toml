# Migration for newvector table
version = 3
mapping_name = "another-vec"
namespace = "anothervec"

[source]
schema = "public"
table = "users"

[id]
column = "id"
type = "uint"

# Optional: filter which rows to sync
# [membership]
# mode = "dsl"
# predicate = "status = 'active'"

[versioning]
mode = "source_lsn"

[transform]
type = "js"
path = "./transforms/antohervec.ts"
