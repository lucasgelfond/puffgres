# Migration for vectortest2 table
version = 2
mapping_name = "vectortest2_public"
namespace = "vectortest2"

[source]
schema = "public"
table = "users"

[id]
column = "id"
type = "uint"

# Optional: filter which rows to sync
# [membership]
# mode = "dsl"
# predicate = "status = 'active'"

[versioning]
mode = "source_lsn"

[transform]
type = "js"
path = "./puffgres/transforms/vectortest2.ts"
