# Migration for pageembed table
version = 1
mapping_name = "pageembed_public"
namespace = "pageembed"

[source]
schema = "public"
table = "page"

[id]
column = "id"
type = "uuid"

# Optional: filter which rows to sync
# [membership]
# mode = "dsl"
# predicate = "status = 'active'"

[versioning]
mode = "source_lsn"

[transform]
type = "js"
path = "./puffgres/transforms/pageembed.ts"
