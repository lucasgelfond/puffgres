# Migration for pageembedinbatch table
version = 1
mapping_name = "pageembedinbatch_public"
namespace = "pageembedinbatch"

[source]
schema = "public"
table = "page"

[id]
column = "id"
type = "uuid"

# Optional: filter which rows to sync
# [membership]
# mode = "dsl"
# predicate = "status = 'active'"

[versioning]
mode = "source_lsn"

[transform]
type = "js"
path = "./puffgres/transforms/pageembedinbatch.ts"
